diff --git a/app/src/main/java/com/mytvb/feature/player/VideoPlayerViewModel.kt b/app/src/main/java/com/mytvb/feature/player/VideoPlayerViewModel.kt
--- a/app/src/main/java/com/mytvb/feature/player/VideoPlayerViewModel.kt
+++ b/app/src/main/java/com/mytvb/feature/player/VideoPlayerViewModel.kt
@@ -1,4 +1,18 @@
     private val playerOkHttpClient = OkHttpClient.Builder()
+        .addInterceptor { chain ->
+            val userAgent = com.mytvb.network.NetworkManager.getCurrentUserAgent()
+            val request = chain.request().newBuilder()
+                // Media CDN requests previously stayed on a hard-coded Chrome/119 identity even
+                // when the API/WBI identity had been refreshed. Keep the media request identity
+                // aligned with the current web session on every open/retry.
+                .header("User-Agent", userAgent)
+                .header("Accept", "*/*")
+                .header("Accept-Language", com.mytvb.network.NetworkManager.getAcceptLanguage())
+                .header("Origin", "https://www.bilibili.com")
+                .header("Referer", "https://www.bilibili.com/")
+                .build()
+            chain.proceed(request)
+        }
         .connectTimeout(5, TimeUnit.SECONDS)
         .readTimeout(15, TimeUnit.SECONDS)
         .connectionPool(okhttp3.ConnectionPool(5, 30, TimeUnit.SECONDS))
@@ -15,13 +29,11 @@
         })
         .build()
     private val upstreamDataSourceFactory = OkHttpDataSource.Factory(playerOkHttpClient)
-        .setUserAgent(
-            "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 " +
-                "(KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36"
-        )
+        .setUserAgent(com.mytvb.network.NetworkManager.getCurrentUserAgent())
         .setDefaultRequestProperties(
             mapOf(
+                "Accept" to "*/*",
                 "Origin" to "https://www.bilibili.com",
-                "Referer" to "https://www.bilibili.com"
+                "Referer" to "https://www.bilibili.com/"
             )
         )
diff --git a/app/src/main/java/com/mytvb/feature/player/PlaybackFallbackController.kt b/app/src/main/java/com/mytvb/feature/player/PlaybackFallbackController.kt
--- a/app/src/main/java/com/mytvb/feature/player/PlaybackFallbackController.kt
+++ b/app/src/main/java/com/mytvb/feature/player/PlaybackFallbackController.kt
@@ -1,11 +1,14 @@
     fun handlePlaybackError(error: PlaybackException, currentPositionMs: Long) {
         lastPlaybackPositionMs = currentPositionMs.coerceAtLeast(0L)
+        val mediaHttpResponseCode = findMediaHttpResponseCode(error)
         val dashSession = context.dashSession
         val streamPlan = context.streamFallbackPlan
         val hasDashFallback = dashSession?.routePlan?.routes?.isNotEmpty() == true && context.useDashPlayback
         val hasProgressiveFallback = streamPlan?.routes?.isNotEmpty() == true
         if (!hasDashFallback && !hasProgressiveFallback) {
-            context.reportError(error.message ?: "加载失败")
+            context.reportError(
+                if (mediaHttpResponseCode == 412) "MEDIA-CDN HTTP 412" else (error.message ?: "加载失败")
+            )
             return
         }
 
@@ -3,6 +3,12 @@
                 ?: streamPlan?.qualityId
                 ?: 0
             AppLog.e(TAG, "fallback exhausted: qualityLocked=$qualityLocked, isDash=$isDash, attempts=$fallbackAttemptCount")
-            context.reportError("当前清晰度下所有线路与编码器都不可用")
+            context.reportError(
+                if (mediaHttpResponseCode == 412) {
+                    "MEDIA-CDN HTTP 412"
+                } else {
+                    "当前清晰度下所有线路与编码器都不可用"
+                }
+            )
         }
     }
@@ -1,3 +1,14 @@
     // ===== 内部 fallback 链 =====
 
+    private fun findMediaHttpResponseCode(error: Throwable?): Int? {
+        var current = error
+        repeat(8) {
+            if (current is androidx.media3.datasource.HttpDataSource.InvalidResponseCodeException) {
+                return current.responseCode
+            }
+            current = current?.cause ?: return null
+        }
+        return null
+    }
+
     private fun classifyPlaybackError(error: PlaybackException): PlaybackErrorType {
